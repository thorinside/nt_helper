{
  "guid": "dels",
  "name": "Delay (Stereo)",
  "description": "This algorithm is a simple stereo in/stereo out delay effect. The delay time can be set manually, via a clock pulse, or via MIDI clock. It can also be modulated via its ‘V/oct input’, which halves the delay time for every 1V rise in CV (conversely, doubles it for every 1V fall in CV). The ‘Fractional’ parameter allows for delay times that are not integer multiples of the sample rate. This slightly increases CPU load but will sound significantly better if the delay time is being modulated, for example for chorus or flange type sounds. Conversely, turning it off makes for an extremely clean digital delay, with no colouration in the repeats.",
  "categories": [
    "audio effects",
    "delay",
    "utility"
  ],
  "short_description": "A simple stereo delay effect",
  "specifications": [
    {
      "name": "Max delay time",
      "min": 1,
      "max": 30
    },
    {
      "name": "Description",
      "value": "The maximum delay time."
    }
  ],
  "parameters": [
    {
      "name": "Mode",
      "min": 0,
      "max": 2,
      "default": 0,
      "description": "The delay mode. The options are “Stereo”, “Stereo<br>ping-pong” (each echo swaps left and right), and<br>“Ping-pong” (the input to the delay is summed to<br>mono, panned, and then swaps left and right on each<br>echo).",
      "type": "string"
    },
    {
      "name": "Mix",
      "min": 0,
      "max": 100,
      "default": 100,
      "description": "The wet/dry mix.",
      "type": "string"
    },
    {
      "name": "Level",
      "min": -40,
      "max": 0,
      "default": 0,
      "description": "The gain applied to the delay/wet signal.",
      "type": "string"
    },
    {
      "name": "Time",
      "min": 1,
      "max": 3276,
      "default": 250,
      "description": "The delay time.",
      "type": "string"
    },
    {
      "name": "Feedback",
      "min": 0,
      "max": 100,
      "default": 50,
      "description": "The delay feedback.",
      "type": "string"
    },
    {
      "name": "Initial pan",
      "min": -100,
      "max": 100,
      "default": -100,
      "description": "If the mode is “Ping-pong”, sets the pan position of<br>the first delay.",
      "type": "string"
    },
    {
      "name": "Delay multiplier",
      "min": 0,
      "max": 23,
      "default": 15,
      "description": "Sets a multiplier to apply to the delay time. Affects<br>both the ‘Time’ parameter and the delay time set by<br>the clock.",
      "type": "string"
    }
  ],
  "input_ports": [
    {
      "name": "Left input",
      "busIdRef": "1",
      "channelCountRef": "28",
      "isPerChannel": true
    },
    {
      "name": "Right input",
      "busIdRef": "1",
      "channelCountRef": "28",
      "isPerChannel": false
    },
    {
      "name": "Left output",
      "busIdRef": "13",
      "channelCountRef": "28",
      "isPerChannel": true
    },
    {
      "name": "Right output",
      "busIdRef": "14",
      "channelCountRef": "28",
      "isPerChannel": false
    }
  ],
  "output_ports": [
    {
      "name": "Output mode",
      "busIdRef": "0",
      "channelCountRef": "1",
      "isPerChannel": true
    }
  ]
}