{
  "guid": "satu",
  "name": "Saturation",
  "description": "This algorithm implements a simple soft-saturation effect. It can be used simply for creative toneshaping, but it is particularly useful for avoiding harsh digital clipping when signals get loud. (It is the same processing that is built into many of the disting EX algorithms as a final “output bus” limiter.)",
  "categories": [
    "audio effects",
    "distortion",
    "dynamics"
  ],
  "specifications": [
    {
      "name": "Channels",
      "description": "The number of bus channels to process, ranging from 1 to 8."
    }
  ],
  "parameters": [
    {
      "name": "Pre gain",
      "min": -40,
      "max": 24,
      "default": 0,
      "type": "enum",
      "is_per_channel": false,
      "is_bus": false
    },
    {
      "name": "Post gain",
      "min": -40,
      "max": 6,
      "default": 0,
      "type": "enum",
      "is_per_channel": false,
      "is_bus": false
    }
  ],
  "input_ports": [
    {
      "name": "Input",
      "description": "The input (and output) bus for saturation processing."
    }
  ],
  "output_ports": [
    {
      "name": "Output",
      "description": "The processed signal after applying soft-saturation, routed from the Input bus."
    }
  ]
}