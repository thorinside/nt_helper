{
  "guid": "btcr",
  "name": "Bit Crusher",
  "description": "This algorithm is an implementation of the disting mk4 algorithm of the same name. The algorithm is a 'bit crusher' – it applies sample rate and sample depth reduction to deliberately introduce quantisation and aliasing artefacts. It also optionally applies bitwise modification of the samples for non-linear distortion effects. It supports two types of bit reduction: Type I (quantised to a 16 bit word, low bits thrown away) and Type II (quantisation via limited precision of integer maths), and allows for symmetric or asymmetric bit reduction. Symmetric bit reduction treats positive and negative sections of the input signal differently by flipping negative sections positive, quantizing, and flipping back.",
  "categories": [
    "Distortion",
    "Lo-Fi",
    "Utility"
  ],
  "short_description": "Sample rate and bit depth reduction",
  "parameters": [
    {
      "name": "Bit reduce",
      "min": 0.0,
      "max": 100.0,
      "default": 0.0,
      "description": "Sets the amount of bit reduction.",
      "type": "file",
      "channelCountRef": "1",
      "is_bus": false
    },
    {
      "name": "Symmetric",
      "min": 0,
      "max": 1,
      "default": 0,
      "description": "Chooses symmetric or asymmetric bit reduction.",
      "type": "enum",
      "is_bus": false
    },
    {
      "name": "Reduction",
      "min": 0,
      "max": 7,
      "default": 0,
      "description": "Sets the bit reduction type. The positive and negative sections of the input signal can have different types of reduction applied.",
      "type": "enum",
      "is_bus": false
    },
    {
      "name": "Mangling",
      "min": 0,
      "max": 7,
      "default": 0,
      "description": "Sets the bit mangling mode.",
      "type": "enum",
      "is_bus": false
    },
    {
      "name": "Downsample",
      "min": 0.0,
      "max": 10.0,
      "default": 0.0,
      "description": "Controls the downsampling (sample rate reduction), scaled at 0.8V/octave.",
      "type": "file",
      "is_bus": false
    },
    {
      "name": "Width",
      "min": 1,
      "max": 8,
      "default": 1,
      "description": "The number of busses to process, starting from the Input. For example, for a stereo signal, set this to 2.",
      "type": "enum",
      "is_bus": false
    },
    {
      "name": "Output mode",
      "min": 0,
      "max": 1,
      "default": 0,
      "description": "The standard Add/Replace mode selector as described above.",
      "type": "enum",
      "is_bus": false
    }
  ],
  "input_ports": [
    {
      "name": "Input",
      "description": "The first input bus to process."
    },
    {
      "name": "Downsample input",
      "description": "The bus to use for the downsample CV, which is added to the Downsample parameter value. (This corresponds to the Y input on the disting mk4 algorithm.)"
    }
  ],
  "output_ports": [
    {
      "name": "Output",
      "description": "The first output bus."
    }
  ]
}