{
  "guid": "usbt",
  "name": "USB audio (to host)",
  "description": "This algorithm routes signals to the USB audio host, if one is connected.\n\nThe host sees 12 input channels. Each one of these can be set to receive any of the disting NT’s busses.\n\nNote that the signals seen by the algorithm, and so by the host, depend where in the list of algorithms this one appears. If it’s the first algorithm, it can send to the host the module’s own inputs, unmodified; if it’s the last algorithm, it can send to the host the outputs of all the other algorithms.\n\nNote also that “USB audio” doesn’t mean that the signals have to be audio; they could also be CVs, if you’d like to exchange signals with, say, VCV Rack or similar.",
  "categories": [
    "Audio",
    "Routing",
    "Interface"
  ],
  "short_description": "Sends audio to USB host",
  "use_cases": [
    "Streaming audio to a DAW",
    "Sending control voltages (CV) to VCV Rack or similar modular environments",
    "Multi-channel signal routing for external processing"
  ],
  "gui_description": "The interface allows mapping each of the 12 USB channels to any bus (1-28) within the disting NT system. Each channel has an adjustable selector parameter.",
  "specifications": [
    {
      "name": "Host Channels",
      "value": "12"
    },
    {
      "name": "Bus Range",
      "value": "1 to 28"
    }
  ],
  "parameters": [
    {
      "name": "USB channel 1 from",
      "min": 1,
      "max": 28,
      "default": 1,
      "type": "enum",
      "is_bus": true
    },
    {
      "name": "USB channel 2 from",
      "min": 1,
      "max": 28,
      "default": 2,
      "type": "enum",
      "is_bus": true
    },
    {
      "name": "USB channel 3 from",
      "min": 1,
      "max": 28,
      "default": null,
      "description": "The bus to send to USB channel 3.",
      "type": "enum",
      "is_bus": true
    },
    {
      "name": "USB channel 4 from",
      "min": 1,
      "max": 28,
      "default": null,
      "description": "The bus to send to USB channel 4.",
      "type": "enum",
      "is_bus": true
    },
    {
      "name": "USB channel 5 from",
      "min": 1,
      "max": 28,
      "default": null,
      "description": "The bus to send to USB channel 5.",
      "type": "enum",
      "is_bus": true
    },
    {
      "name": "USB channel 6 from",
      "min": 1,
      "max": 28,
      "default": null,
      "description": "The bus to send to USB channel 6.",
      "type": "enum",
      "is_bus": true
    },
    {
      "name": "USB channel 7 from",
      "min": 1,
      "max": 28,
      "default": null,
      "description": "The bus to send to USB channel 7.",
      "type": "enum",
      "is_bus": true
    },
    {
      "name": "USB channel 8 from",
      "min": 1,
      "max": 28,
      "default": null,
      "description": "The bus to send to USB channel 8.",
      "type": "enum",
      "is_bus": true
    },
    {
      "name": "USB channel 9 from",
      "min": 1,
      "max": 28,
      "default": null,
      "description": "The bus to send to USB channel 9.",
      "type": "enum",
      "is_bus": true
    },
    {
      "name": "USB channel 10 from",
      "min": 1,
      "max": 28,
      "default": null,
      "description": "The bus to send to USB channel 10.",
      "type": "enum",
      "is_bus": true
    },
    {
      "name": "USB channel 11 from",
      "min": 1,
      "max": 28,
      "default": null,
      "description": "The bus to send to USB channel 11.",
      "type": "enum",
      "is_bus": true
    },
    {
      "name": "USB channel 12 from",
      "min": 1,
      "max": 28,
      "default": null,
      "description": "The bus to send to USB channel 12.",
      "type": "enum",
      "is_bus": true
    }
  ],
  "input_ports": [],
  "output_ports": []
}