{
  "guid": "saml",
  "name": "Sample Player (Sliced)",
  "description": "This algorithm allows you to place a number of marker points on a sample and conveniently trigger playback from the various marker points. It’s your go-to algorithm for breakbeat slicing, enabling precise control over sample segments via MIDI notes or CV/gate inputs while supporting features like automatic loop synchronization with tempo, transient detection, and customizable slice routing.",
  "categories": [
    "sampling",
    "routing",
    "MIDI",
    "triggers",
    "clocking"
  ],
  "short_description": "Plays sliced samples",
  "use_cases": [
    "Breakbeat slicing",
    "Precise sample triggering via MIDI or CV/gate",
    "Automatic loop synchronization to tempo",
    "Transient-based slice marker placement",
    "Customizable playback speed and pitch for individual slices"
  ],
  "gui_description": "The display shows the sample waveform with slices indicated as check marks above. The last triggered slice is displayed bottom right, alongside sample length (seconds), bars at current tempo, and looped bar count if clock and auto-trigger are enabled.",
  "specifications": [
    {
      "name": "Record time",
      "min": 0,
      "max": 60
    }
  ],
  "parameters": [
    {
      "name": "Slice",
      "scope": "sample rate",
      "type": "enum",
      "is_common": true,
      "is_bus": false
    },
    {
      "name": "Slices",
      "min": 1,
      "max": 24,
      "default": 24,
      "scope": "sample rate",
      "type": "enum",
      "is_common": true,
      "is_bus": false
    },
    {
      "name": "Speed",
      "min": -200.0,
      "max": 200.0,
      "default": 100.0,
      "scope": "sample rate",
      "type": "enum",
      "is_bus": false
    },
    {
      "name": "Transpose",
      "min": -60,
      "max": 60,
      "default": 0,
      "scope": "sample rate",
      "type": "enum",
      "is_bus": false
    },
    {
      "name": "Fine tune",
      "min": -100,
      "max": 100,
      "default": 0,
      "scope": "sample rate",
      "type": "enum",
      "is_bus": false
    },
    {
      "name": "MIDI base note",
      "description": "Sets the MIDI note number for slice 1. Subsequent slices are triggered by following MIDI notes (e.g., C4, C#4, D4). The MIDI note below this stops playback.",
      "type": "enum"
    },
    {
      "name": "Stop at slice end",
      "min": 0,
      "max": 1,
      "default": 0,
      "description": "If enabled (1), playback will stop when it reaches the next slice. Otherwise (0), playback continues to the end of the sample.",
      "type": "enum"
    },
    {
      "name": "Clock source",
      "min": 0,
      "max": 2,
      "default": 0,
      "description": "Sets clock source: 'Off' (0), 'External' (1), or 'MIDI' (2).",
      "type": "enum"
    },
    {
      "name": "Auto trigger",
      "min": 0,
      "max": 2,
      "default": 0,
      "description": "Sets whether the sample will automatically be triggered by clock: 'Off' (0), 'Fixed interval' (1), or 'Loop length' (2).",
      "type": "enum"
    },
    {
      "name": "Auto divisor",
      "min": 0,
      "max": 19,
      "default": 15,
      "description": "Sets how often the sample will be triggered if 'Auto trigger' is set to 'Fixed'.",
      "type": "enum"
    },
    {
      "name": "Time sig num",
      "min": 1,
      "max": 99,
      "default": 4,
      "description": "The time signature numerator (e.g., 4 for 4/4).",
      "type": "enum"
    },
    {
      "name": "Time sig denom",
      "min": 0,
      "max": 4,
      "default": 2,
      "description": "The time signature denominator: one of '1' (0), '2' (1), '4' (2), '8' (3), or '16' (4).",
      "type": "enum"
    },
    {
      "name": "Coarse",
      "min": 0.0,
      "max": 100.0,
      "default": 0.0,
      "description": "The coarse slice position, as a percentage of the sample length.",
      "type": "enum"
    },
    {
      "name": "Fine",
      "min": -500,
      "max": 500,
      "default": 0,
      "description": "The fine slice position, in sample frames (adjusts precise timing).",
      "type": "enum"
    },
    {
      "name": "Play",
      "scope": "sample rate",
      "type": "enum",
      "is_bus": false
    },
    {
      "name": "Always include start",
      "description": "If enabled, a slice marker will always be added for the start of the file, even if the markers in the sample file do not include one."
    }
  ],
  "input_ports": [
    {
      "name": "Slice input",
      "description": "CV/gate bus for selecting slices (V/oct, scaled at one semitone per slice)."
    },
    {
      "name": "Trigger input",
      "description": "Gate/trigger input to start playback of a selected slice."
    },
    {
      "name": "Advance input",
      "description": "Trigger input to advance the active slice by one (looping back to 1 after reaching max slices)."
    },
    {
      "name": "Reset input",
      "description": "Trigger input to reset playback to slice 1."
    },
    {
      "name": "Slice input",
      "description": "The bus to use for the slice CV input (V/oct, scaled at one semitone per slice)."
    },
    {
      "name": "Clock input",
      "description": "The bus to use for the clock input (24 ppqn)."
    },
    {
      "name": "Run/stop input",
      "description": "The bus to use for the run/stop input."
    },
    {
      "name": "Advance input",
      "description": "The bus to use for the advance input (to step through slices)."
    },
    {
      "name": "Reset input",
      "description": "The bus to use for the reset input (resets playback to slice 1)."
    }
  ],
  "output_ports": [
    {
      "name": "Audio output",
      "description": "Outputs the sliced sample audio."
    }
  ]
}